## Example JSON

```json
{ 
   "txid": "...a transaction id",
   "block": "...the block hash in which this tx resides",
   "index": 12,
   "leaves": [
      "...a leaf of the merkle path",
      "...a leaf of the merkle path",
      "...a leaf of the merkle path",
      "...a leaf of the merkle path",
   ]
}
```
